# Cloudflare Pages 配置
# 部署后需在 Dashboard → Pages → Settings → KV namespace bindings 绑定 NEKOWIKI_KV

pages_build_output_dir = "dist"
name = "nekowiki"
compatibility_date = "2025-07-31"

# KV 绑定请在 Cloudflare Dashboard 中配置:
#   Pages → Settings → Functions → KV namespace bindings
#   添加绑定: 变量名 NEKOWIKI_KV, 选择对应的 KV namespace
